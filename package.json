{
  "name": "carbon-typst-blog",
  "private": true,
  "scripts": {
    "build": "node lib/node/build-html.mjs",
    "build:force": "npm run build -- --force",
    "build:fast": "npm run build -- --jobs 8",
    "build:preview": "npm run build -- --out _site-preview",
    "build:full": "npm run build -- --force --jobs 8"
  }
}
